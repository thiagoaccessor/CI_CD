name: CI/CD

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login no Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Selecionar subscription
        run: az account set --subscription 8565d32b-e115-4870-9c70-f0fdb4235689

      - name: Testar login
        run: |
          az account show
          az account list --output table

      - name: Deploy para Azure WebApp
        uses: azure/webapps-deploy@v3
        with:
          app-name: front-testes001
          package: ./frontend   # <-- aponta para a pasta onde estão index.html e styles.css
          slot-name: production
